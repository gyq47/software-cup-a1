-r requirements-core-py39.txt

# Optional layers are intentionally not installed by default on LoongArch.
# Install only after the Python 3.9 core deployment is verified:
#   python -m pip install -r requirements-rag-optional.txt
#   python -m pip install -r requirements-extra-optional.txt
